package org.adligo.i.smtp.models;

import org.adligo.tests.ATest;

public class LocalFileEMailAttachmentTests extends ATest {

	public void testLocalFileEMailAttachment() {/*
		RAMEMailAttachment attach = new RAMEMailAttachment("build.xml", "text/plain");
		assertEquals("build.xml",attach.getFileName());
		assertEquals("text/plain", attach.getMimeType());
		assertEquals(100, attach.getData().length);
		*/
	}
}
